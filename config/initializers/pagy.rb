# Pagy default configuration file
# See https://ddnexus.github.io/pagy/docs/api/pagy/#configuration for the full list of variables.
#
# No extras (bootstrap/navs/etc.) are required yet — Pagy::Frontend's default
# `pagy_nav` helper works out of the box with the app's existing daisyUI styling
# (plain <a> tags, easy to restyle later if needed). Add extras here if/when
# something beyond the default nav is needed.

# Pagy::DEFAULT[:limit] = 20 # default (items per page)
