<% content_for :title, "Edit #{@customer.full_name}" %>

<%= render partial: "shared/page_header", locals: {
      title: "Edit #{@customer.full_name}",
      breadcrumbs: [ [ "Customers", customers_path ], [ @customer.full_name, customer_path(@customer) ] ]
    } %>

<%= render "shared/form_container" do %>
  <%= render "form", customer: @customer %>
<% end %>
