<%# Mobile top bar - only shown below the lg breakpoint once the drawer shell lands in
    Step 6. The hamburger's for="app-drawer" targets the drawer-toggle checkbox that Step 6
    adds to the layout - inert (points at nothing) until then. %>
<div class="navbar bg-base-100 border-b border-base-300 lg:hidden">
  <label for="app-drawer" class="btn btn-square btn-ghost" aria-label="Open menu">
    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true" focusable="false">
      <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </label>
  <span class="font-semibold text-base-content ml-2">Jobdeck</span>
</div>
