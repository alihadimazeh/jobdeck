<% content_for :title, "Edit #{@job.title}" %>

<%= render partial: "shared/page_header", locals: {
      title: "Edit #{@job.title}",
      breadcrumbs: [ [ "Jobs", jobs_path ], [ @job.title, job_path(@job) ] ]
    } %>

<%= render "shared/form_container" do %>
  <%= render "form", job: @job %>
<% end %>
